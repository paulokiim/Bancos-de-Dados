const db = require("../db");

// index, show, store, update, destroy
module.exports = {
    index(req, res) {
        //Constroi a query e retorna o res
    },
    show(req, res) {},
    store(req, res) {},
    update(req, res) {},
    destroy(req, res) {}
};
